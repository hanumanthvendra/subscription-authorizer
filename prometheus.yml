global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: subscription-authorizer
    metrics_path: /metrics
    static_configs:
      - targets: ["authorizer:8080"]
